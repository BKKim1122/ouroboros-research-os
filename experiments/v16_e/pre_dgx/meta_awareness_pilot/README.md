# V16 Attention–Meta-Awareness Pilot

This is an exploratory successor track to the frozen V1–V15 manuscript/evidence package. It replaces the earlier embodied-factorization V16 priority with a direct test of primary attention, internal attention monitoring, report access, and closed-loop recovery. The embodied identity/ownership/agency design is retained as a later-stage plan.

Run:

```bash
python src/v16_meta_awareness.py --root . --mode all --steps 700
```

Pilot outputs are not confirmatory evidence. A new protocol and exact frozen source snapshot must be created before confirmatory seeds are run.
